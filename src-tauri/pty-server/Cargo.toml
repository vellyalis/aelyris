[package]
name = "aether-pty-server"
version = "0.2.3"
edition = "2021"

[dependencies]
aether_terminal_lib = { package = "aether-terminal", path = ".." }
log = "0.4"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
