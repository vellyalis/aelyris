# Security vulnerabilities must NOT be filed as issues. See SECURITY.md and use
# GitHub Private Vulnerability Reporting (enable it in repo Settings > Security).
# No contact_links are configured here on purpose: a hardcoded URL would be wrong
# until the public repository slug exists, and a broken link would mislead the
# responsible-disclosure flow. SECURITY.md is surfaced automatically by GitHub.
blank_issues_enabled: true
